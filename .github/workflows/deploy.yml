name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip' # Caching pip dependencies
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Run tests with coverage
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        DATABASE_URL: ${{ secrets.DATABASE_URL }}
      run: |
        pytest --cov=modules --cov=database --cov-report=xml
        
    - name: SonarCloud Scan
      uses: SonarSource/sonarcloud-github-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  # publish-to-jfrog:
  #   needs: test
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   steps:
  #   - uses: actions/checkout@v4
    
  #   - name: Setup JFrog CLI
  #     uses: jfrog/setup-jfrog-cli@v4
  #     env:
  #       JF_URL: ${{ secrets.JF_URL }}
  #       JF_USER: ${{ secrets.JF_USER }}
  #       JF_PASSWORD: ${{ secrets.JF_PASSWORD }}

  #   - name: Docker Login to JFrog
  #     run: |
  #       echo "${{ secrets.JF_PASSWORD }}" | docker login ${{ secrets.JF_DOCKER_REGISTRY }} -u ${{ secrets.JF_USER }} --password-stdin

  #   - name: Build and Push Docker Image to JFrog
  #     run: |
  #       docker build -t ${{ secrets.JF_DOCKER_REGISTRY }}/finbot-pro:${{ github.sha }} .
  #       docker push ${{ secrets.JF_DOCKER_REGISTRY }}/finbot-pro:${{ github.sha }}
  #       docker tag ${{ secrets.JF_DOCKER_REGISTRY }}/finbot-pro:${{ github.sha }} ${{ secrets.JF_DOCKER_REGISTRY }}/finbot-pro:latest
  #       docker push ${{ secrets.JF_DOCKER_REGISTRY }}/finbot-pro:latest

  #   - name: Publish Build Info to JFrog
  #     run: |
  #       jf rt build-publish finbot-pro ${{ github.run_number }}

  # Trigger rebuild for deployment
  deploy:
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Install Koyeb CLI
      run: |
        curl -fsSL https://raw.githubusercontent.com/koyeb/koyeb-cli/master/install.sh | sh
        echo "$HOME/.koyeb/bin" >> $GITHUB_PATH

    - name: Trigger Koyeb Redeploy
      env:
        KOYEB_TOKEN: ${{ secrets.KOYEB_TOKEN }}
      run: |
        # Menggunakan CLI untuk trigger redeploy
        koyeb service redeploy continuous-mehetabel/finbot
